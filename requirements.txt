# =============================================================================
# PRODUCTION REQUIREMENTS (Railway / Docker)
# =============================================================================
# This file is intentionally minimal to keep Docker image under 4GB.
# For local development with HuggingFace reranker, use: pip install -r requirements-dev.txt
#
# NOTE: Do NOT add torch, transformers, or sentence-transformers here.
# Those packages add 2GB+ to the image size.
# =============================================================================

# Core Flask dependencies
flask>=3.1.0
flask-socketio>=5.5.1

# Production server
gunicorn>=21.2.0
eventlet>=0.33.0

# LLM and AI dependencies
# IMPORTANT: Use base package only (no [all] extra) to avoid pulling in torch/transformers
ai2-scholar-qa>=0.5.8
openai>=1.0.0
langsmith>=0.3.32

# PDF and document processing
pymupdf>=1.26.0
anyascii>=0.3.2

# Utilities
python-dotenv>=1.0.0
retry>=0.9.2
loguru>=0.7.3

# Additional dependencies (may be required by sub-dependencies)
pyyaml>=6.0
werkzeug>=3.0.0
python-socketio>=5.10.0
